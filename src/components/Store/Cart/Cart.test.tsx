import React from 'react';
import { render } from '@testing-library/react';
import Cart from '.';

describe('<Cart />', () => {
  it('should render Cart correctly', () => {
    render(<Cart />);

  })
});


