import React from 'react';
import { render } from '@testing-library/react';
import Cart from '.';

describe('<Cart />', () => {
  it('should reder Cart correctly', () => {
    render(<Cart />);

  })
});


