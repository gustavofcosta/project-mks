import React from 'react';
import { render } from '@testing-library/react';
import Store from '.';

describe('<Store />', () => {
  it('should reder Store correctly', () => {
    render(<Store />);

  })
});


