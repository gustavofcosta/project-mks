import React from 'react';
import { render } from '@testing-library/react';
import CardList from '.';

describe('<CardList />', () => {
  it('should reder CardList correctly', () => {
    render(<CardList />);

  })
});


